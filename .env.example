# Environment Variables
#
# Copy this file to .env and fill in real values.
# NEVER commit .env to source control — it is already in .gitignore.
#
# All VITE_* variables are embedded at build time by Vite.
# Non-VITE_ variables are for tooling (Docker, CI, etc.) only.

# ---------------------------------------------------------------------------
# API
# ---------------------------------------------------------------------------

# Base URL for the FastAPI backend.
# Leave empty (default) to use the built-in mock API — no backend required.
# Example: http://localhost:8000  or  https://api.yourcompany.com
VITE_API_BASE_URL=

# ---------------------------------------------------------------------------
# Polling
# ---------------------------------------------------------------------------

# How often (in milliseconds) the dashboard polls for new device metrics.
# Default: 3000 (3 seconds)
VITE_POLLING_INTERVAL_MS=3000

# ---------------------------------------------------------------------------
# Alerts
# ---------------------------------------------------------------------------

# Optional webhook URL for critical anomaly notifications.
# When set, the app will POST alert payloads to this URL.
# Example: https://hooks.slack.com/services/XXX/YYY/ZZZ
VITE_ALERT_WEBHOOK_URL=

# ---------------------------------------------------------------------------
# Anomaly Detection (advanced — matches src/config.js defaults)
# ---------------------------------------------------------------------------

# Number of rolling samples kept per device metric for Z-score calculation.
# Default: 20
VITE_ANOMALY_WINDOW_SIZE=20

# Z-score threshold above which a metric is flagged as anomalous.
# Default: 2.5  (2.5 standard deviations from the rolling mean)
VITE_ANOMALY_Z_THRESHOLD=2.5
