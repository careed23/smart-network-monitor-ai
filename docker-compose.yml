version: '3.9'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    # Map host port 3000 → container port 80 (nginx)
    ports:
      - '3000:80'
    environment:
      # Override these at runtime or via a .env file (see .env.example)
      - VITE_API_BASE_URL=${VITE_API_BASE_URL:-}
      - VITE_POLLING_INTERVAL_MS=${VITE_POLLING_INTERVAL_MS:-3000}
    restart: unless-stopped
